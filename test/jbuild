(executable
 ((name test)
  (libraries (pecu alcotest fmt))))

(alias
 ((name runtest)
  (package pecu)
  (deps (test.exe (glob_files "contents/*")))
  (action (run ${exe:test.exe} --color=always))))
